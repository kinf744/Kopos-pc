<UserControl x:Class="KighmuVpnWindows.UI.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundDarkBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Logs" FontSize="18" FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="Effacer" Click="Clear_Click"
                    Style="{StaticResource MaterialDesignFlatButton}" />
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource KighmuCard}">
            <ScrollViewer x:Name="LogScroll" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="LogOutput" Style="{StaticResource KighmuLogText}"
                           Text="En attente de connexion..." />
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
